# Firstly install all dependencies via the following command:
pip3 install -r req.txt

# Now train the application in your pc as:
python diabetes.py

# Finally run the application :
python gui.py

# Now insert all the requires details and enjoy!!